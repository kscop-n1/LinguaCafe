# LinguaCafe 0.5.22

## Changed
- Exported the websocket app key from the server-rendered layouts and removed the hardcoded frontend constant from the Echo bootstrap.
- Kept the key in sync for both the user shell and auth shell so the browser bundle reads a server-provided value before initialization.

## Verified
- The login HTML now renders `window.__LINGUACAFE_WEBSOCKET_APP_KEY` before `@vite`.
- The authenticated shell exposes the same key to the browser runtime.
- The live browser reports `window.__LINGUACAFE_WEBSOCKET_APP_KEY === "wjp2pou6ebgibtwccqsj"` and the app still boots with `window.__LINGUACAFE_BOOTSTRAP_ERROR === null`.
