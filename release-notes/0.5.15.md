# LinguaCafe 0.5.15

## Changed
- Migrated the user-settings theme color table from legacy `v-simple-table` to native Vuetify 3 table markup.
- Kept the theme color editing, picker, hex input, and reset actions intact.
- Verified the `/user-settings` themes tab live after deployment.
- Confirmed the browser session stayed clean with `window.__LINGUACAFE_BOOTSTRAP_ERROR === null` on the validated route.
