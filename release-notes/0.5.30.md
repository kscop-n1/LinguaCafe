# LinguaCafe 0.5.30

## Fixed
- Fixed transparent popup, dropdown, menu and date picker overlays after the Vue/Vuetify migration.
- Replaced remaining old Vuetify 2 theme CSS variable usage with Vuetify 3 theme variables.
- Fixed misplaced overlays by binding Vuetify 3 activator props consistently.
- Fixed left navigation hover and active states so text remains readable.
- Migrated remaining legacy overlay/menu props and list menu contents to supported Vuetify 3 patterns.

## Added
- Added a static migration test for legacy overlay props, theme variables, v-model listeners, activator bindings and related UI migration regressions.

## Verified
- ./vendor/bin/phpunit --filter VueMigrationStaticTest
- npm run production
- Browser verification on https://lingua.lan for dropdown surfaces, date picker anchoring, left navigation hover state and console/runtime errors.
