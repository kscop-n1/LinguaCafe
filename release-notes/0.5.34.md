# LinguaCafe 0.5.34

## Fixed
- Fixed opened dropdown activator buttons so they no longer become black when the cursor moves from the button into the opened dropdown menu.
- Extended Vuetify overlay normalization to `aria-expanded="true"` menu buttons, covering opened activators in addition to hover and focus states.

## Verified
- npm run production
- ./vendor/bin/phpunit --filter VueMigrationStaticTest
- Browser verification on https://lingua.lan for hover button -> open dropdown -> hover menu item.
