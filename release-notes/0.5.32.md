# LinguaCafe 0.5.32

## Fixed
- Fixed the remaining migrated Vuetify active states in dropdowns, overlay menus, selected tabs and active buttons so the overlay layer no longer turns controls black or hides their text.
- Replaced the old overlay menu active-color override with theme primary/on-primary colors for readable selected items in both light and dark themes.

## Verified
- npm run production
- ./vendor/bin/phpunit --filter VueMigrationStaticTest
- Browser verification on https://lingua.lan for active dropdown menu items and active buttons/tabs.
