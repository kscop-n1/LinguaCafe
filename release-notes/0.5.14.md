# LinguaCafe 0.5.14

## Changed
- Migrated the remaining reader vocabulary panels from legacy `v-tabs-items` / `v-tab-item` to native Vuetify 3 window items.
- Covered `TextReaderSettings.vue`, `VocabularyBox.vue`, `VocabularySideBox.vue`, and `VocabularyBottomSheet.vue` in the same reader flow.
- Verified the reader route and settings dialog live after deployment, including the `/chapters/read/4` fixture.
- Confirmed the browser session stayed clean with `window.__LINGUACAFE_BOOTSTRAP_ERROR === null` on the validated route.
