# LinguaCafe 0.5.29

## Changed
- Removed the orphaned `resources/vue3` / PrimeVue frontend tree from the repository so there is only one shipped frontend path.
- Updated the migration audit to reflect that the alternate frontend track is no longer present.

## Verified
- `resources/vue3` no longer exists in the working tree.
- The shipped frontend still builds from `resources/js/app.js`.
