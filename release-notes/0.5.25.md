# LinguaCafe 0.5.25

## Changed
- Backfilled the existing-user `password_changed` state in the live database so migrated users are no longer forced into the change-password gate.
- Corrected the upgrade docs so they no longer claim the migration is fully complete while cleanup items remain.

## Verified
- The live database row for the existing user now reports `password_changed = true`.
- `UPGRADE_MIGRATION_PHASES.md` and `UPGRADE_AUDIT_REPORT.md` now say the migration is implemented with remaining cleanup tracked in `release-notes/migration-audit.md`.
