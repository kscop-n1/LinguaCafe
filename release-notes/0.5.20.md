# LinguaCafe 0.5.20

## Changed
- Removed the dead Vuetify 2 selection shims from the app bootstrap (`v-list-item-group`, `v-list-item-avatar`, `v-list-item-content`).
- Updated the theme selection dialog to use the migrated Vuetify 3 list markup and reload the app after a theme change so cached theme-dependent screens reinitialize.
- Verified live that switching the authenticated theme to `eink` or `light` persists the cookie/localStorage state and comes back up cleanly after reload.
