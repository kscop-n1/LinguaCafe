# LinguaCafe 0.5.23

## Changed
- Added a root `package-lock.json` so the shipped frontend dependency graph is tracked and reproducible.
- Left the Vue 3 alternate frontend tree locked separately, but the deployable root app now has a lockfile too.

## Verified
- `npm install --package-lock-only --ignore-scripts --no-audit` completed successfully at the repo root.
- The generated `package-lock.json` reflects the current shipped frontend dependency graph.
