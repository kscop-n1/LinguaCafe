# LinguaCafe 0.5.13

## Changed
- Migrated TextReaderSettings from legacy `v-tabs-items` / `v-tab-item` to native Vuetify 3 window items.
- Kept the reader settings tab switching behavior intact on the live reader route.
- Verified the dialog in the browser on a processed chapter fixture at `/chapters/read/4`.
- Confirmed the live route still boots cleanly with `window.__LINGUACAFE_BOOTSTRAP_ERROR === null`.
