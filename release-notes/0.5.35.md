# LinguaCafe 0.5.35

## Added
- Integrated PrimeVue Aura theme components and updated frontend design infrastructure.
- Added automated migration audit checks for codebase health (verifying legacy Vue 2 syntax, package dependencies, and CSS selectors).

## Removed
- Cleaned up legacy Vue 2 compiler/runtime APIs, styles, and obsolete assets.
- Removed redundant Bootstrap, jQuery, and Popper.js frontend dependencies.

## Verified
- npm run production
- Node.js scripts: `scripts/check-legacy.js`, `scripts/check-deps.js`, and `scripts/check-css-legacy.js`
