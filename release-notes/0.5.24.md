# LinguaCafe 0.5.24

## Changed
- Removed the remaining Vuetify 2 table/tab compatibility shims from the app bootstrap and migrated the remaining `v-simple-table` call sites to native `v-table`.
- Verified the migrated `user-settings` themes tab renders with `v-table` wrappers and no bootstrap errors.

## Verified
- `rg` finds no remaining `v-simple-table`, `v-tabs-items`, or `v-tab-item` references in the app source.
- Live browser on `https://lingua.lan/user-settings` shows two `v-table` wrappers on the Themes tab and `window.__LINGUACAFE_BOOTSTRAP_ERROR === null`.
