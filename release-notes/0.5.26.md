# LinguaCafe 0.5.26

## Changed
- Removed the dead `currentTheme` and `breakpoint` Vuetify adapter shims from the bootstrap.
- Kept the app on native Vuetify 3 theme/display APIs while the remaining compatibility work continues in the components that still use legacy props.

## Verified
- `rg` no longer finds `currentTheme`/`breakpoint` adapter shims in `resources/js/app.js`.
- The live browser still boots cleanly on `/user-settings` and the migrated `v-table` views render without bootstrap errors.
