# LinguaCafe 0.5.46

## Fixed

- Fixed the Chapters table Items per page dropdown drifting upward inside chapter rows after scrolling or switching to taller page sizes.
- Added viewport-based positioning for the Vuetify footer select menus in both the Library book details Chapters table and the reader chapter list dialog.
- Preserved the corrected chapter pagination totals and existing 10, 25, 50, and All page-size behavior.

## Verification

- Verified in browser with a 60-chapter fixture at 10 rows and 50 rows; the menu opens above the selector with sub-pixel vertical gap and aligned left edge.
- Verified after responsive resize at 768px width.
- Added a static migration regression test for the chapter table footer menu positioning contract.
- Passed `./vendor/bin/phpunit --filter VueMigrationStaticTest` in the dev webserver container.
- Passed `npm run check:migration`.
- Passed `git diff --check`.
